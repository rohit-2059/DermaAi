[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 300 --preload"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
